---
import { getCollection } from 'astro:content';

const stories = await getCollection('stories');
---

<h1>Short Stories</h1>
<ul>
  {stories.map((story: { slug: any; data: { title: unknown; author: unknown; date: unknown; }; }) => (
    <li>
      <a href={`/stories/${story.slug}`}>{story.data.title}</a>
      <p>by {story.data.author} — {story.data.date}</p>
    </li>
  ))}
</ul>

